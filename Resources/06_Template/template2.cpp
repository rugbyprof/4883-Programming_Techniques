#include <iostream>
#include <deque>
#include <vector>
#include <string>
#include <algorithm>

#define endl "\n"

#define DEBUG 1
#define debug(k,v) (if(DEBUG){cout<<k<<":"<<v<<endl;})


using namespace std;

int i = 0, C = 0, N = 0, T = 0, M = 0, A = 0;
string S;

int main() {


    cin >> C;
    while (i < C) {
        cin >> N >> T >> M;

        for (int m = 0; m < M; ++m) {

        }
        i++;
    }
    return 0;
}